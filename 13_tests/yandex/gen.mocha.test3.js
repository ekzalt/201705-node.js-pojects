// hook
describe('some test', function () {
  before(function () {
    // run this code one time before all tests
  });

  beforeEach(function () {
    // run this code many times before each test
  });

  /*
  // run some tests
  it();
  it();
  it();
  */

  after(function () {
    // run this code one time after all tests
  });

  afterEach(function () {
    // run this code many times after each test
  });
});