// handle to process.stdout
/*
const fs = require('fs');
try {
  const file = fs.readFileSync('txt.txt');
} catch (err) {
  process.stdout.write(err);
}
*/

// call back error

// wrap to Promise

// make async func

// listeners
server.on('error', err => {
  //
});